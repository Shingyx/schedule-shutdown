{
  "name": "schedule-shutdown",
  "version": "1.1.3",
  "description": "Schedules shutdowns for your computer",
  "repository": "https://github.com/Shingyx/schedule-shutdown",
  "license": "MIT",
  "author": "Su-Shing Chen",
  "files": [
    "dist/!(test)"
  ],
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": "dist/cli.js",
  "scripts": {
    "prebuild": "yarn clean",
    "build": "tsc",
    "build-test": "yarn build && yarn test",
    "clean": "rimraf dist",
    "format": "yarn tslint --fix && yarn prettier --write",
    "prepare": "yarn build",
    "prepublishOnly": "yarn test",
    "prettier": "prettier --list-different \"src/**/*.ts\" \"*.md\"",
    "test": "nyc --reporter=text --reporter=lcov mocha --require source-map-support/register \"dist/**/*.test.js\"",
    "posttest": "yarn tslint && yarn prettier",
    "tslint": "tslint --project tsconfig.json"
  },
  "dependencies": {
    "tslib": "^1.9.3",
    "yargs": "^12.0.5"
  },
  "devDependencies": {
    "@types/chai": "^4.1.7",
    "@types/mocha": "^5.2.6",
    "@types/node": "^10.12.29",
    "@types/sinon": "^7.0.8",
    "@types/yargs": "^12.0.9",
    "chai": "^4.2.0",
    "mocha": "^6.0.2",
    "nyc": "^13.3.0",
    "prettier": "^1.16.4",
    "rimraf": "^2.6.3",
    "sinon": "^7.2.6",
    "source-map-support": "^0.5.10",
    "tslint": "^5.13.1",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^3.3.3333"
  }
}
